apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: portfolio
  namespace: portfolio
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`gaelrozario.com`) || Host(`www.gaelrozario.com`)
      kind: Rule
      services:
        - name: portfolio
          port: 80
