name: My first job

on:
  - push

jobs:
  job1:
    runs-on: arc-runner-portfolio
    steps:
      - name: Welcome message
        run: echo "Welcome to the new job"

      - name: List files
        run: ls -al

      - name: cat Dockerfile
        run: cat Dockerfile
