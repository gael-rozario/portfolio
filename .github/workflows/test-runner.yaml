name: First workflow to print Dockerfile

on:
  - push

jobs:
  ReadDockerfile:
    runs-on: arc-runner-portfolio
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Dragon say hello
        run: |
          ls -al
          cat Dockerfile
          cat /etc/*release*
          sudo apt-get update
          sudo apt-get install cowsay
          cowsay -f dragon "hello, world"
